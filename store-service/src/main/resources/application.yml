spring:
  application:
    name: store-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
  config:
    import: "configserver:"

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info
